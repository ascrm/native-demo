import { StyleSheet } from "react-native"

export const defaultStyle=StyleSheet.create({
  container:{
    flex:1,
    justifyContent:'center',
    alignItems:'center',
    backgroundColor:'#333'
  },
  text: {
    fontSize: 20,
    color: '#fff',
  }
})
